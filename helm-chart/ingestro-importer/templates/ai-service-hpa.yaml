{{- if and .Values.aiService.enabled .Values.aiService.autoscaling.enabled }}
{{- $root := . }}
{{- $component := "aiService" }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "ingestro-importer.componentFullname" (dict "root" $root "component" $component "suffix" "hpa") }}
  labels:
    {{- include "ingestro-importer.componentLabels" (dict "root" $root "component" $component) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "ingestro-importer.componentFullname" (dict "root" $root "component" $component) }}
  minReplicas: {{ $root.Values.aiService.autoscaling.minReplicas }}
  maxReplicas: {{ $root.Values.aiService.autoscaling.maxReplicas }}
  metrics:
    {{- if $root.Values.aiService.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ $root.Values.aiService.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if $root.Values.aiService.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ $root.Values.aiService.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}